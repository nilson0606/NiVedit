# NiVedit v12.12 原始碼

## v12.12：保留一般辨識，短分段改為選用（2026-09-19）

- 使用者指出先前影片在平衡模型正常，不能把單一影片測出的參數當成通用最佳解。辨識預設恢復原本 30 秒／重疊 5 秒。
- AI 字幕新增「辨識方式」：一般（預設）／短分段重試。手動選擇後者才使用 20 秒／重疊 2 秒。設定保留於目前頁面，不存入專案，重新載入預設回一般；不依檔名或台詞特判。
- 保留 v12.11 的 Turbo FP16 載入、取消與錯誤处理、進度計數修正。
- Edge 實測同一影片：一般模式重現前 5 秒輸出；手動短分段可到 24.42 秒。這是補救選项而非全面提升準確度的承諾。已驗證切換回一般、頁面重載預設、日夜、英文及模型重用計數。
- 操作手冊同步說明。公開音樂仍 41 首／認可清單 43 首；私有影片、專案、SRT 和歷史還原點不修改。

## v12.11：AI 字幕修正（2026-09-19）

- Whisper 使用 20 秒、2 秒重疊分段，修正實測影片遇停頓後只輸出前 5 秒的情況。單段長度不是影片長度上限。
- large-v3-turbo 在支援 FP16 的 WebGPU 上優先載入 FP16（約 1.6GB），不再優先選需要 2.55GB 外部權重的 FP32 encoder。
- 修正取消後重試的 watchdog、重用模型的分段計數與錯誤回報。紀錄包含模型、音訊長度、載入方式及最後階段。
- 驗證：Edge 原生解碼的 24.47 秒影片，small 辨識至 24.42 秒，turbo FP16 至 24.46 秒；完整介面及取消／重試測試通過。仍需人工校對，非完整編輯器回歸測試。

## v12.10：獨立 YT → MP3 入口（2026-09-18）

- 主頁「操作手冊」旁新增「YT → MP3」，開新分頁。独立 Windows 本機下載器支援單支 YouTube、128/192/320 kbps、進度、取消、試聽、另存與日夜模式；完成後從「＋音軌 → 匯入電腦音檔」加入。
- GitHub Pages 僅提供入口、安裝包與說明；每台電腦需先啟動本機工具。setup.cmd 需 Python 3.10+、Windows x64，建立獨立 venv 並安裝 yt-dlp；Node LTS／FFmpeg 先尋找現有版本，缺少才下載。
- 本機服務固定 http://127.0.0.1:19627/，GET 首頁注入本次權杖；API 保留 Host／Origin／權杖檢查，不開 CORS。單一服務鎖保護，主頁無跨來源掃描。
- 本機 NiVedit.cmd 會以 --no-browser 啟動已配置的 yt-mp3-local；直接開 HTML 或網站則依說明啟動工具。公開包只含程式／說明，不含下載音樂、session.json、config.json、本機路徑或第三方二進位。
- 維護來源：nivedit-src/yt-mp3-helper/、yt-mp3.html、yt-mp3-assets/；build.py 複製入口與工具包。操作手冊音軌章節已更新。
- Edge 整合 15 項通過（本機 file 與網站子路徑、新分頁、工具 ZIP、MP3 匯入、日夜、來源／權杖保護）；i18n 41/41。新資料夾初始化成功，沿用現有 Node／FFmpeg，未在另一台乾淨 Windows 重測下載分支。
- 先前獨立工具已實測公開短片下載、MP3 轉檔、播放、跳轉與瀏覽器下載；本次未重跑無關完整剪輯測試。內建曲庫仍為原 40＋1 首；重要還原點仍為 v12.5。


## v12.9：恢復最初 40＋1 首內建配樂（2026-09-18）

- 依使用者最終選擇，現行內建曲庫恢復為配樂 v2：原有 40 首＋授權範例曲，共 41 首；撤下曲庫中的 v3／v4 新增 60 首，停止這輪配樂製作。
- 曲目、ID、排列、MP3 音訊及 41 首下載 ZIP 均沿用原版本。四類數量恢復 11／10／10／10；每首约 40 秒。匯入電腦音檔功能保留。
- 僅還原音樂目錄、文案與下載入口；裁切框內／框外、相容轉碼、手冊等現有剪輯功能保留。已保存專案的內嵌音訊維持原樣。
- 使用者偏好為原 40＋1 首；v3／v4 僅屬歷史版本，不是現行曲庫。既有封存和素材檔保留作歷史，不列在內建素材庫。
- 操作手冊及發布說明更新，原 41 首雜湊與解碼逐一核對。針對性檢查 music-example 68、i18n 41，共 109 項；不宣稱完整功能套件重測。
- 證據 qa-music-restore41-v12.9/；來源 archive/NiVedit_原始碼_v12.9.tar.gz。重要還原點仍為 v12.5。

## v12.8：依回饋重寫 60 首情緒配樂（2026-09-18）

- 使用者反映 v3 新增 60 首比原有 40 首單調，要求自由發揮、情緒多元且每首編曲骨架差異要大；不以單一參考曲套用整批。
- 以配樂 v4 取代曲庫中的 60 首 v3 曲目；原有 v2 四十首及授權範例音訊保持原樣，總數仍為 101。已存 .nvproj 使用內嵌音訊，不會被素材庫更新換歌。
- 60 首各有獨立 A/B 旋律與實際段落序列，31 種配器／節奏風格，4/4、3/4、6/8 拍。主奏與副主奏交替、對答、間奏、留白、逐層加入、主題回歸與收尾；不是只替換樂器或移調。
- 情緒共 12 種，各 5 首：開心、俏皮、希望、放鬆、浪漫、懷念、自信、冒險、振奮、感傷、懸疑、壯闊。曲庫可直接搜尋情緒；原有四大分類保留，數量 26／25／25／25。
- 全部約 40 秒，192 kbps MP3 立體聲；GeneralUser GS 取樣音色編曲合成，非真人演奏。音訊驗證只能確認技術品質，是否好聽仍以使用者實際試聽為準。
- 音乐製作來源 tools/music_v4_compositions.py、tools/build_music_pack_v4.py；v4-041～v4-100 為替換新曲，v2- ID 不變。合集 assets/NiVedit_Music_101_40s_v4.zip，包含離線試聽頁、曲目／樂譜資料及音源授權。
- Edge 驗證：music-example 70/70、情緒搜尋／離線頁／真實 H.264+AAC 音樂匯出 10/10、i18n 41/41，合計 121 項；全 101 首逐一 SHA256 與解碼核對，未重跑無關完整測試庫。
- 驗證與發布證據 qa-music-v4/；完整來源 archive/NiVedit_原始碼_v12.8.tar.gz。既有重要還原點 v12.5 不變，v12.7 封存保留。

## v12.7：新增 60 首多元配樂，合集 101 首（2026-09-18）

- 新增 60 首原創無人聲背景音樂，每首約 40 秒；保留原有 40 首及授權範例曲的檔名、識別與音訊，合計 101 首。
- 新曲 12 種曲風各 5 首：木吉他民謠、巴薩、海島雷鬼、爵士、慵懶嘻哈、東方禪意、放克、合成器、浩室、靈魂樂、三拍華爾滋、電影敘事。不同節奏、和弦、旋律、段落和收尾，使用 GeneralUser GS 取樣樂器編曲合成，非真人演奏。
- 素材庫維持四大分類：輕快日常 26 首，其餘三類各 25 首。60 首新曲排在前面；搜尋「新曲」可只找新增曲目。「匯入電腦音檔」保留。每首按需載入，不會一次下載整包。
- 合集 assets/NiVedit_Music_101_40s_v3.zip 包含 101 個 MP3、四類資料夾、離線搜尋試聽頁、曲目表、作曲事件與音源授權。網站同包位於 background-music/。舊包及舊素材網址保留。
- 來源 tools/build_music_pack_v3.py；新曲 ID v3-041～v3-100；原 v2- 曲目 ID 不變。產生器沿用 v2 的取樣渲染與音源 SHA256，正常 build.py 不需音樂製作套件。
- Edge 70 項音樂／範例檢查（含全 101 首 SHA256 與解碼）、9 項搜尋／離線頁／實際 H.264+AAC 匯出、41 項 i18n 回歸，共 120 項通過。未重跑無關完整功能套件；試聽喜好由使用者挑選。
- 主程式 v12.7、手冊配樂章節同步。完整來源 archive/NiVedit_原始碼_v12.7.tar.gz；測試與發布證據 qa-music-v3/。重要還原點仍為 v12.5；不覆寫历史封存。

## v12.6：矩形／圓形保留框內或框外（2026-09-18）

- 畫面裁切增加「保留範圍」：保留框內（預設）／保留框外。選不裁切時不顯示此選項。
- cropKeep 為 inside／outside，保存至 .nvproj，復原重做與分割沿用；舊專案缺值或非法值歸為 inside。
- 30_render.js 在素材本地座標以 evenodd 建立外框加洞，再共同套用縮放／位置／透明度／旋轉。框內透明會露出下方影片／圖片層；不直接擦掉合成後的底圖。
- 圓形直徑 0：框內隱藏，框外保留全圖。矩形寬高 1.00：框外全隱藏。尺寸與中心仍可設關鍵幀；形狀與保留範圍整段固定。
- Windows Edge：crop 62/62、dual-track 45/45、i18n 41/41，共 148 項；包含 4 種 MP4 真實匯出後解碼對照預覽。未重跑全部測試庫。
- 操作手冊構圖章節同步更新；主程式 v12.6。本機與網站發布來源同步，重要還原點仍為 v12.5，不覆寫歷史封存。


## v12.5：影片相容提醒、轉碼工具與新版手冊（2026-09-17）

- 原因確認：Chrome 關閉 GPU 時，HEVC 可能只讀到音訊，readyState=4 且 error=null，但 videoWidth/videoHeight 都為 0。不能只檢查 error 或 WebCodecs 就緒。
- 24_compat.js 檢查實際影像尺寸與媒體錯誤，預覽下方顯示素材檔名。等待首幀超過 15 秒也會提示等待。loadeddata/seeked 到達時重畫，修正慢解碼超過原本 700ms 重畫窗口的情況。
- 舊專案中解碼失敗的影片保留 File、元素及所有剪輯欄位，不再直接跳過。新匯入完全失敗時，開啟獨立工具供使用者處理原檔。
- 獨立「轉成相容格式」：選專案片段或自己的影片，保留原尺寸或最高 1080p（直式依方向），使用 Worker 內的 @ffmpeg/core 0.12.10 軟體轉成 H.264/AAC。首次工具約 43 MB，按需載入；file:// 與 HTTPS 子路徑均可用，不用跨來源隔離、不上傳影片。
- 轉碼可取消／失敗重試；先驗證輸出能解碼才顯示下載與套用。套用只替換選取片段，配置新 ID 保留 MEDIA 中原素材供復原／重做；不改原檔、不自動存檔，使用者仍需儲存或另存。下載得到獨立 MP4。
- 限制：單檔 512 MiB，像素總數上限 4096×4096；4K CPU 轉碼可能數分鐘。保留第一條音訊並編為 AAC，其餘音軌／拍攝附加資料不帶入。HDR PQ/HLG 經 tonemap 轉 SDR BT.709；輸出會重新壓縮，不宣稱無損。
- 匯出先攔下無影像／解碼錯誤素材；逐幀備援不再逾時後直接當成功，會檢查 readyState、尺寸及 seek 完成，10 秒無影像則中止並列出檔名。失敗時清理編碼器與解碼器。
- 新增 video-compat/ 為第四個隨 HTML 配送的工具資料夾，包含核心與授權、對應版本來源碼及建置資訊。manifest.json 驗證核心 SHA256；核心 GPL-2.0-or-later，來源提供於同目錄 sources/。不要只拷貝主 HTML。一般 build.py 不需下載網路依賴。
- HTML 操作手冊 16 章，左側章節導覽、單章顯示、搜尋、日夜與完整列印；主畫面「操作手冊」使用 target=_blank、rel=noopener 開新分頁。build.py 一併輸出 manual.html。本機另保留 NiVedit_操作手冊.html。手冊編輯來源 manual/，截圖已改用 manual/images/，不再依賴 QA 路徑。
- Chrome 153 關閉 GPU：compat.e2e 52/52、compat-extras 18/18；真實 25 秒 4K HEVC 約 195 秒完成，輸出 3840×2160 H.264/AAC、預覽可用、復原／重做正常，剪輯設定只更新縮圖和素材識別。合成 HDR 直式／無聲片驗為 8-bit BT.709 SDR。
- Edge 154 回歸：dual-track 45/45、rotation-save 71/71、music-example 68/68、i18n 41/41，共 225 項。本次共 295 項通過；未重跑無關全部套件。先前 layers 標題像素容差基線差異仍保留，不宣稱全套已清除。
- QA 在 qa-chrome-compat/。私人專案、原影片、相容副本及截圖不發布；v12.2 重要還原點及所有舊封存保留。

NiVedit 是瀏覽器端影音編輯器。主程式為 NiVedit.html，內建動畫素材位於旁邊的 animation-effects/；影片、音訊、字幕與專案都在使用者電腦處理，不上傳。

## 背景音樂與範例專案（v12.4，2026-09-17）

目前內建音樂已替換為 **配樂 v2：40 首新曲＋1 首授權範例曲，每首約 40 秒**，版本標示仍為 v12.4（素材更新）。使用者覺得 v1 音色單調，指定 example.nvproj 裡的「開心輕快_40秒.mp3」作風格參考，並同意延長到 40 秒。新曲全部重新編曲，保留明確主題、呼應樂句、轉折與收尾；分類改為輕快日常、悠閒舒服、活力律動、溫暖故事，各 10 首。以情緒／用途選曲，不再以細分樂器為主。範例原有配樂不變。「開啟範例」成功後會固定回到檔頭 00:00 並保持暫停；首次載入與快取重開都相同。一般使用者專案仍保留自己存檔時的播放位置。

v2 使用 GeneralUser GS v2.0.3 的取樣樂器，由 TinySoundFont 渲染，再以 ffmpeg 製成 44.1 kHz／立體聲／192 kbps MP3，約 −17 LUFS。不是真人樂團錄音，也未複製參考曲音訊。生成器 tools/build_music_pack_v2.py，音源版本／雜湊／許可在 tools/music-v2-notices/；音源本體只保存在本機 QA 工具資料夾，不隨網站下載。新包 assets/NiVedit_Music_41_40s_v2.zip，離線試聽頁 assets/music_pack_41_v2/index.html。40 首原創新配樂之外，使用者於 2026-09-17 明確授權加入「開心輕快（範例配樂）」原音檔；音源說明僅適用那 40 首新曲，範例原音檔另附授權來源紀錄。曲譜事件與逐曲音訊檢查也在包內。

28_music_catalog.js 新增 MUSIC_PACK，卡片時長與下載檔名讀索引，避免寫死 25 秒。目錄列 40 首 v2 新曲與 1 首授權範例配樂（共 41 首）；舊 v1 payload／ZIP 保留供舊快取頁面相容，舊 MP3 素材包與 v12.4 原始封存不覆寫，已存專案仍嵌入原音檔。此次驗證在 qa-music-v2/，68 項音樂／範例檢查包含兩種載入路徑、41 首真實解碼和範例安全另存；另做實際 MP4 匯出與語系檢查。本次未重跑無關的全部剪輯回歸；前次完整結果見 qa-v12.4/。

左側「開啟範例」先顯示說明，再按「載入範例」才下載；不會在進站時取代使用者專案。範例影片素材為 720p，明確提醒正式專案最高可設定為 4K；升高解析度不會把 720p 素材變成原生 4K 細節。使用者授權公開的 example.nvproj 原檔保持 4K、完全不改；公開副本由 tools/build_example_project.py 轉成約 16.6 MiB，保留原時間軸、標題、兩句字幕、GIF、配樂與專案設定。只有影片素材轉碼為 1280×720。

46_example_catalog.js 與 46_example.js 載入 example-assets/ 的 JS payload，build.py 複製至 example-project/。下載完成後先驗 SHA-256，再交既有 projImportFile。開啟的是沒有 writable handle 的 File 副本（_fh=null），所以首次「儲存」與「另存」都要求使用者選新檔案；預設名稱 NiVedit_範例練習.nvproj。存好後只更新使用者檔案，內建範例從不作為寫入目標。重新開啟範例仍是原始內容。不必先新建專案。已有未存修改時，載入前提醒先儲存；取消不動時間軸。

本機與 Pages 都需要 HTML 旁的 animation-effects/、background-music/、example-project/ 三個資料夾。不可只發布主 HTML；素材按需載入，已加入專案的音樂／GIF 都嵌入 .nvproj，可離線重開。v12.2 重要還原點不動。

本次新功能 62／62、動畫效果 65／65；既有計數回歸 729／730，另有 GIF 與兩支語系診斷完成。唯一失敗仍為 Edge 154 的標題像素容差（預覽 84／解碼 67），與原封 v12.2 相同，未放寬閾值。

驗證紀錄在 qa-v12.4/：新增 music-example.e2e.cjs 驗證 file:// 與網站子路徑、音樂試聽／匯入／重試、40 首實際解碼、範例首次儲存／再次儲存／另存／重開原始範例及使用者存檔。儲存選擇器以測試 handle 取代，實際跑序列化、寫入、暫存與素材重接；未自動操作 Windows 原生檔名視窗。另實際匯出含內建音樂的 MP4，AAC 音訊非靜音且無削波。

## 動畫效果素材庫（v12.3）

「＋ 動畫效果」提供兩種來源：內建 240 組素材，或「匯入自己的 GIF／圖片」。四包分別為表情與慶祝 40、常用效果 50、短影音效果 100、誇張效果 50；可搜尋、依素材包／分類篩選，先預覽再加入播放頭位置。選取後的分割按鈕為「動畫效果切割」。

本機雙擊 HTML 與 GitHub Pages 都可使用。建置時自動把 effect-assets/ 複製成輸出 HTML 旁的 animation-effects/，分發本機版時兩者一起帶走。第一次載入網站素材需要連線；GIF 加入專案後沿用既有 .nvproj 封裝，不依賴網站重開。

正常建置只需 Python 3：python build.py [輸出HTML路徑]。來源包已含素材，不需原始素材包或 Pillow。只有重新產生素材時，才執行 tools/build_effect_assets.py [原始assets資料夾]，並安裝 Pillow。請勿把含本機路徑與提示詞的原始 manifest 直接發布。

內部 overlay／over 資料欄位維持相容。預覽採縮圖與單一 GIF 圖片；加入時間軸才解碼動畫。保留影片剪輯、關鍵幀、匯出、PNG／GIF 本機匯入等既有功能。

## 歷史版本紀錄

## v9.7

字幕編輯器新增「編輯軌道」下拉選單。選上軌只顯示上軌，選下軌只顯示下軌；目前軌道沒有字幕時顯示 0 句及新增提示，不再列出另一軌。＋一句明確使用 _seTrack，保留新增後聚焦，空白上軌也能直接建立。從時間軸選取字幕再開啟時跟隨其軌道。

整批取代、轉台灣正體、清空本軌及編輯器的存成 SRT 都限定目前軌道。清空需確認，復原可還原；SRT 檔名加 _upper／_lower，原生儲存與下載備援都只包含所選軌。編輯器外原有整份 SRT 匯出仍保留。原 AI 字幕不會因編輯另一軌被改動。

Edge：subtitle-scope 26、subtitle-edit 20、subtitles 27、i18n 41，共 114 項通過。驗證空白上軌新增、連續新增、兩軌批次隔離、清空取消及復原、換軌、原生 SRT 寫入與下載、專案重開及雙字幕 MP4 匯出；日夜截圖已檢查。其他無關測試本次未重跑。

## v9.6

修正字幕編輯器「＋ 一句」後仍反白舊 AI 字幕：新增後選取、捲到並聚焦新句，預設文字全選可直接輸入；重新開啟編輯器時跟隨時間軸選取，關閉時同步右側欄位。編輯器只排序顯示副本，不再更動字幕原始順序；同軌合併仍按時間找下一句。字幕／疊圖方塊點選不再同時拖動播放頭。

同軌時間重疊的字幕仍沿用原本第一筆有效字幕優先的預覽／匯出行為，沒有刪除、替換或搬動既有 AI 字幕。右側顯示重疊提示，可調整時間或分到上下字幕軌。這與編輯器選取問題分開處理。

Edge：subtitle-edit 20、subtitles 27、i18n 41，共 88 項通過；另有 GIF 16 組專項通過，字幕與 GIF 實際 MP4 匯出正常。qa-v9.6/reproduction.json 保存 v9.5 的重現證據；日夜截圖已檢查。其他無關測試本次未重跑。

## v9.5

影片／圖片片段、PNG／GIF 疊圖、標題的起點與終點旋轉滑桿統一為 −360°～360°，可跨兩端旋轉兩圈。影片「畫面方向」的 90° 轉向快捷鍵維持原設計。

影片編碼完成後，按「選擇位置並儲存」開啟原生另存視窗，可選資料夾與檔名。Windows／Edge 原生視窗負責同名覆蓋確認；不覆蓋時改名再儲存。取得 handle 後才 createWritable，write／close 完成才顯示已儲存；取消或失敗保留編碼結果可重試，寫入失敗會 abort。每次另存都重新開啟選擇器，不會直接重用 handle 跳過覆蓋確認。不支援 showSaveFilePicker 的瀏覽器維持下載備援。

Edge：rotation-save 46、motion 67、title-rotation 15、i18n 41、end-align 90、volume-curve 20，共 279 項通過；另有 GIF 16 組專項與實際 MP4 匯出通過。新儲存測試以 picker adapter 搭配真實 OPFS 串流測試取消、拒絕權限、寫入／關閉失敗、覆蓋、改名及 MP4 解碼；Windows 原生覆蓋提示未做自動化點擊。日夜畫面已檢視，其餘無關測試本次未重跑。

## v9.4

影片／圖片片段、PNG／GIF 疊圖及標題在關鍵幀開啟後，都有「快速對位（終點）」九宮格。只改終點 x/y，保留起點、其他動畫屬性、緩動及時間窗；暫停時立即重繪，預覽仍依目前播放頭時間顯示插值。沿用紫色終點底色，支援日夜及中英模式。上方原有九宮格仍保留 v8.0 的整條路徑平移行為。

Edge 驗證：end-align 90、motion 67、keyframe-ui 24、title-rotation 15、i18n 41，共 237 項通過；另有 GIF 16 組專項通過，包含實際 MP4 匯出。本次未重跑其餘無關功能測試。

## v9.3

依使用者更正，僅「音量曲線」區塊的標題與說明加強顯示。以 volume-curve-help 類別限定範圍；其餘音軌說明與標題恢復 v9.1 原樣。Edge 已比對日夜模式，確認曲線外文字樣式與 v9.1 完全一致。

## v9.2

音軌屬性面板說明改用日夜主題的主文字色，字級 11 → 13px、行高 1.75；區塊標題 14px／粗體。樣式僅作用於音軌面板。v9.2 已驗證日夜對比、切換範圍、音量點操作，並通過 i18n 41 項與 volume-curve 20 項；v9.1 全套回歸結果保留為基準。

## v9.1

標題大小移到起點構圖區，起終點現在同為水平、垂直、大小、不透明度、旋轉五項。
旋轉與進退場動畫合成，日夜底色、存讀、分割與匯出同步。

## v9.0

標題起點補上不透明度，與終點同為水平、垂直、大小、不透明度四項。
進場／退場動畫標題改為高對比粗體與藍色底線，日夜同步。

## v8.9

關鍵偵開關標題改為 15px 高對比粗體及藍色底線，開關文字與勾選框加大，日夜同步。
時間軸預設高度由 480 降至 440px，讓分隔線再往下移，仍可拖曳調整。

## v8.8

時間軸預設略縮為 480px，軌道列微縮，仍可拖分隔線調整。
三個影片轉場標題改為 15px 粗體、高對比文字與藍色底線，跟隨日夜模式。

## v8.7

所有素材的動態開關描述統一為「關鍵偵開啟/關閉」，依使用者原文。
起終點控制與日夜配色保持，英文同步為 Keyframes on/off。

## v8.6

圓形裁切直徑可為 0，支援起點／終點，縮至零完全隱藏。
已設關鍵幀的項目以藍底起點、紫底終點及 ◆ 標記，跟隨日夜模式；影片、疊圖、標題共用。
啟用動態會採用剛調整後的目前數值。

## v8.5

時間軸為上影片、上字幕、下影片、下字幕，字幕兩軌各有樣式與位置。
所有影片一律可分離留白；每段內扣／外加只控制片段首尾轉場：
內扣使用原片動態時間，外加延長第一幀／最後一幀的定格時間，定格無原聲。
內扣／外加及淡入／淡出／聲音一起都在片段面板獨立設定；上軌淡出露出下軌。
淡化只影響該段畫面及原聲，配樂與字幕等不受影響。.nvproj 保留完整設定。
舊整軌模式複製到各片段；舊整片淡化移至每軌首段／末段，開舊案應確認頭尾效果。

## v8.2

影片／圖片片段新增矩形／圓形裁切，寬高或直徑與裁切中心可設定兩點式關鍵幀，
和大小、位置、透明、旋轉一起作用。形狀整段固定。詳見交接文件與裁切測試。

## v8.1

影片軌的影片與圖片片段新增「起點 → 終點」兩點式動態：

- 水平與垂直位置
- 大小
- 不透明度
- 旋轉
- 四種緩動
- 可設定動態開始與結束區間
- 時間軸菱形標記、九宮格快速對位、分割、復原／重做與 `.nvproj` 存讀

影片原有的 0／90／180／270 度「畫面方向」仍負責素材方向校正；動態旋轉使用獨立欄位，避免破壞直立手機影片。沒有啟用動態時，v8.0 與舊專案的畫面維持原樣。

目前不做多點關鍵幀。疊圖、標題與影片都維持起點、終點兩點式操作。

## 建置

在 `D:\NiVedit` 執行：

```powershell
python nivedit-src/build.py
```

輸出為根目錄的 `NiVedit.html`。第三方函式庫已放在 `vendor/`，不需下載 npm 套件。

`build.py` 會先由 `src/dict_en.tsv` 產生 `src/06_dict.js`，再依固定順序合併 `src/`。新增原始碼檔案時也要更新 `build.py` 的載入清單。

## 測試

測試說明在 `tests/README.md`。v9.1 的十一支計數式回歸共 357 項：

- i18n：41
- 軌道排序：18
- 配樂音量曲線：20
- 動態：67
- 標題旋轉：15（五項控制同組、實際畫面／MP4 角度、分割與存讀）
- 裁切：36（含兩種形狀 MP4 解碼像素比對）
- 關鍵幀介面：24（零直徑、日夜底色、存讀與 MP4 無殘點）
- 每段選項：35（獨立模式／淡化、即時預覽、遷移及 MP4 畫面／AAC 音訊）
- 雙軌：45
- 首尾轉場與留白：29（動態／定格、存讀、MP4 畫面／音訊）
- 雙字幕與交錯四列：27（樣式、拖曳、存讀、MP4）

另有 i18n 掃描、語言來回與 GIF 現行版專項測試。v9.1 已在 Edge 153 全部通過，動態測試會實際量影片畫面並匯出影片。
