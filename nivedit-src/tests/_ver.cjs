/* 每一支測試都會斷言「畫面上的版號 === 這裡寫的版號」，
   用來確認你測到的真的是剛建置出來的那一份，不是瀏覽器或容器留下的舊檔。

   v11.9 以前這個值是寫死在十七支測試裡的字串，改版靠一行

       sed -i "s/v11\.8/v11.9/g" tests/*.cjs

   ——那一行會把**註解裡的版號一起改掉**。於是 `rotation-save.e2e.cjs` 裡
   「v11.1 存檔改成先寫暫存檔再改名」這種歷史紀錄，每發一版就往前跳一版，
   現在整批都指著當下的版號，全部失真。發現的時候已經追不回原值了。

   改成一個檔案之後：改版號只動這裡，`sed` 那條規矩作廢，註解不會再被波及。
   （歷史註解的版號要不要考古還原，列在交接說明的待辦裡。） */
module.exports = { VER: 'V13.2' };
